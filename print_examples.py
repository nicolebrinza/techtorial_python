#"Hello" "World"
# Concatening the texts
# We can use +(plus) sign or we can use ,(comma)

print("Hello"+" World")
# plus sign will concatenate the string(text) as it is

print("Hello", "World")
# comma sign will concatenate the string(texts) and will put a space between them
